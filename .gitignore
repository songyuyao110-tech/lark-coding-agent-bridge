node_modules/
dist/
coverage/
*.log
*.tsbuildinfo

.env
.env.local
.env.*.local
.npmrc

.DS_Store
.idea/
.vscode/

# Local maintenance scripts and unreleased changelog are not part of the package
scripts/
CHANGELOG.md
tests-smoke/

# Local-only dev files: machine-specific MCP config, and a stray npm lockfile
# (this repo installs with pnpm).
.mcp.json
package-lock.json

# Archived draft tests that no longer compile against the current API.
*.superseded
